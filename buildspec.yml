version: 0.2

phases:
  install:
    commands:
      - echo "nothing to install for static HTML"
  build:
    commands:
      - echo "No build step needed for static site"

artifacts:
  files:
    - appspec.yml
    - index.html
    - scripts/**/*
